# {{ .Name }}

{{ .Orchestrator.Description }}

## Architecture

**Pattern**: {{ .Orchestrator.Pattern.String }} ({{ .Orchestrator.Pattern.Description }})

**Orchestrator**: {{ .Orchestrator.Name }}

**Sub-Agents**:
{{- range .Orchestrator.SubAgents }}
- **{{ .Name }}**: {{ .Type }}
{{- end }}

## Installation

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp {{ .PackageName }}/.env.example {{ .PackageName }}/.env
```

## Usage

```bash
adk run {{ .PackageName }}
```

## Web Interface

```bash
adk web --port 8000
```

Run `adk web --port 8000` from the project root, then open http://localhost:8000 and select `{{ .PackageName }}`.

## Project Structure

```
{{ .Name }}/
├── {{ .PackageName }}/
│   ├── __init__.py
│   ├── .env.example
│   ├── agent.py
│   └── sub_agents/
{{- range .Orchestrator.SubAgents }}
│       └── {{ snakeCase .Name }}/
│           ├── __init__.py
│           └── agent.py
{{- end }}
├── requirements.txt   # Python dependencies
└── README.md          # This file
```

## About ADK

This project uses Google's Agent Development Kit (ADK) for building multi-agent systems.

## Generated by

Agent Builder - A CLI tool for creating ADK multi-agent systems.
