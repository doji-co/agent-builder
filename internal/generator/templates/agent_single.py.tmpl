from google.adk.agents import LlmAgent

agent = LlmAgent(
    name="{{ snakeCase .Name }}",
    model="{{ .Model }}",
    instruction="{{ .Instruction }}",
    {{- if .OutputKey }}
    output_key="{{ .OutputKey }}",
    {{- end }}
)
